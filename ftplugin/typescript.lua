local bo = vim.bo

bo.tabstop = 2 -- size of a hard tabstop (ts).
bo.shiftwidth = 2 -- size of an indentation (sw).
bo.expandtab = true -- always uses spaces instead of tab characters (et).
bo.softtabstop = 2 -- number of spaces a <Tab> counts for. When 0, feature is off (sts).
